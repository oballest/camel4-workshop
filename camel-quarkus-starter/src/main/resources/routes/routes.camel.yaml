- route:
    from:
      uri: "direct:ruta-prueba"
      steps:
        - to: mock:ingreso-ruta
        - transform:
            simple: "${body}-transformacion"
        - to: mock:salida-ruta
      
   
      